import { ProductPriceModel } from './product-price-model';

describe('ProductPriceModel', () => {
  it('should create an instance', () => {
    expect(new ProductPriceModel()).toBeTruthy();
  });
});
