import { ShoppingListModel } from './shopping-list-model';

describe('ShoppingListModel', () => {
  it('should create an instance', () => {
    expect(new ShoppingListModel()).toBeTruthy();
  });
});
