import { ShoppingListProductModel } from './shopping-list-product-model';

describe('ShoppingListProductModel', () => {
  it('should create an instance', () => {
    expect(new ShoppingListProductModel()).toBeTruthy();
  });
});
