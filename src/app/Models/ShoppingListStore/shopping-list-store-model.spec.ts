import { ShoppingListStoreModel } from './shopping-list-store-model';

describe('ShoppingListStoreModel', () => {
  it('should create an instance', () => {
    expect(new ShoppingListStoreModel()).toBeTruthy();
  });
});
